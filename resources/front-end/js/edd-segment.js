;(function( $, sa, undefined ) {

	sa.Segment = {
		config: {},
	};

	/**
	 * methods
	 */
	sa.Segment.init = function() {

	};


})( jQuery, window.sa = window.sa || {} );

// Init
jQuery(function() {
	sa.Segment.init();
});